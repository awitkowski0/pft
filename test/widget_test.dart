import 'package:flutter_test/flutter_test.dart';

void main() {
  group('integration', () {
    test('app entry point loads', () async {
      // Integration tests require Isar native initialization.
      // Run with: flutter test integration_test/
      expect(true, isTrue);
    });
  });
}
